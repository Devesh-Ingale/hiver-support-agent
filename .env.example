# Copy to .env (git-ignored). Only needed to re-run the judge or the Gemini comparison row.
# Free key: https://aistudio.google.com/apikey
GEMINI_API_KEY=

# Optional: Ollama server if not on the default localhost:11434
# OLLAMA_HOST=http://localhost:11434
